# GitHub Repository Secrets Setup Configuration
# Copy this file to .env and update with your actual values

# AWS Role ARNs for GitHub Actions OIDC
# Get these values from: cd ../iam-foundation && terraform output
AWS_ROLE_ARN_DEV="arn:aws:iam::YOUR_ACCOUNT_ID:role/github-actions-dev-role"
AWS_ROLE_ARN_PROD="arn:aws:iam::YOUR_ACCOUNT_ID:role/github-actions-prod-role"

# GitHub Organization and Repositories
GITHUB_ORG="CDAIDavidson"
GITHUB_REPOS="CDAIDavidson/terraform-template,CDAIDavidson/scaffold_aws_bda_doc_parser"

# Optional: GitHub CLI profile (if using multiple accounts)
# GITHUB_PROFILE="your-username"

# Optional: Custom commit message for workflow files
# COMMIT_MESSAGE="Add CI/CD workflows for GitHub Actions"

# Optional: Branch name for workflow commits (default: main)
# BRANCH_NAME="main"
